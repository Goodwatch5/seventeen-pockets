services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - seventeen-pockets-data:/app/data
    env_file:
      - path: .env
        required: false
    restart: unless-stopped

volumes:
  seventeen-pockets-data:
